from django.apps import AppConfig


class ChartsConfig(AppConfig):
    """Configuration for the charts app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "charts"
    verbose_name = "Charts"
