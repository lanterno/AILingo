# AILingo

Full-stack application with Django REST API backend and React frontend for AI-powered tutoring.

## Structure

- `api/` - Django REST API backend
- `web/` - React frontend
- `terraform/` - Infrastructure as code for GCP deployment
